## Travel Tracker Frontend.

**Running tests:** `npm test` (uses Vitest; run `npm install` first if `node_modules/` is absent).



This is the frontend application for my travel tracker project, which visualizes my trip to Japan using an interactive map. It's built with vanilla JavaScript (ES6 modules) and uses the Leaflet library for mapping functionalities.

There's no point reading this README - [come check out the map directly here instead!](https://seitaro-white.github.io/travel-tracker-frontend/)